# Optional. Defaults to the local TinyCloud node during development.
VITE_TINYCLOUD_HOST=http://127.0.0.1:8000

# Required outside development to authorize migration-key rotation.
SHAPE_DEMO_ADMIN_TOKEN=replace-with-a-private-admin-token
